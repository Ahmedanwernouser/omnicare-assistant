# Exact pins, not ranges. The LangChain/LangGraph ecosystem ships breaking
# changes on a two-week cadence; an unpinned build that works today can fail
# for a reviewer next week. Regenerate with `pip freeze` after any upgrade.

# --- Validation & settings -------------------------------------------------
pydantic==2.13.4
pydantic-settings==2.15.0

# --- Persistence -----------------------------------------------------------
filelock==3.32.2

# --- RAG -------------------------------------------------------------------
# Bundles all-MiniLM-L6-v2 via onnxruntime: no API key, no paid service.
# The weights are NOT in the wheel - scripts/warm_embeddings.py caches them at
# image build time.
chromadb==1.5.9

# --- Agent -----------------------------------------------------------------
langgraph==1.2.11
langchain-core==1.5.4

# Provider SDKs: install only the one LLM_PROVIDER names. Uncomment as needed.
langchain-groq==1.1.3
# langchain-openai==1.1.2
# langchain-anthropic==1.2.0

# --- API -------------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.1

# --- Tests -----------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1          # required by fastapi.testclient
