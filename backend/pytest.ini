[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: needs the real embedding model (skipped when not cached)
filterwarnings =
    ignore::DeprecationWarning
